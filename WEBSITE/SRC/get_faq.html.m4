include(`general.m4')
dnl -------------------------------------------------------------
set_page_title([Frequently Asked Questions])
define([id1], [get])
define([id2], [faq])
define([id3], [])

include_header

dnl -------------------------------------------------------------

section([Fortran Compilers])
skip_line

section([External libraries])
skip_line

section([MPI])


skip_line


dnl -------------------------------------------------------------
include_footer
