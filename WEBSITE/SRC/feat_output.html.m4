include(`general.m4')
dnl -------------------------------------------------------------
set_page_title([Solver features - Output functions and files])
define([id1], [feat])
define([id2], [output])
define([id3], [])

include_header

dnl -------------------------------------------------------------

section([Output files])
item([VTK])
item([Tecplot])
<br>

section([Monitoring])

skip_line

dnl -------------------------------------------------------------
include_footer
